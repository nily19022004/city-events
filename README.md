# Информационная система планирования городских мероприятий

Веб-приложение для публикации и управления городскими мероприятиями. Система позволяет просматривать афишу предстоящих мероприятий и управлять событиями через административную панель.

## 🎯 Основные возможности

- 📅 **Публичная афиша мероприятий** - просмотр предстоящих городских мероприятий
- 🔍 **Поиск мероприятий** - быстрый поиск по названию или месту проведения
- 📱 **Адаптивный дизайн** - удобный просмотр на всех устройствах (десктоп, планшет, мобильный)
- 👤 **Административная панель** - полнофункциональное управление мероприятиями:
  - Добавление новых мероприятий
  - Редактирование существующих мероприятий
  - Деактивация/активация мероприятий
  - Удаление мероприятий
  - Поиск по списку мероприятий

## 🛠 Технологии

- **Backend**: Python 3.10+, Django 4.2+ / 6.0
- **Frontend**: HTML5, CSS3, Bootstrap 5 (CDN)
- **База данных**: SQLite (для разработки, легко переходит на PostgreSQL)
- **Иконки**: Bootstrap Icons

## 📋 Требования

- Python 3.10 или новее
- pip (менеджер пакетов Python)
- Git (для клонирования репозитория)

## 🚀 Установка и запуск

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/nily19022004/city-events.git
cd city-events
```

### Шаг 2: Создание виртуального окружения

**Для Windows (PowerShell):**
```powershell

python -m venv venv
.\venv\Scripts\Activate.ps1
```

**Для Windows (Command Prompt):**
```cmd

python -m venv venv
venv\Scripts\activate.bat
```

**Для Linux/macOS:**
```bash

python3 -m venv venv
source venv/bin/activate
```

После активации виртуального окружения в начале строки терминала появится `(venv)`.

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Применение миграций

Создание структуры базы данных:

```bash
python manage.py migrate
```

### Шаг 5: Создание суперпользователя

Создайте учетную запись администратора для доступа к административной панели:

```bash
python manage.py createsuperuser
```

Вам будет предложено ввести:
- Имя пользователя (username)
- Email (опционально)
- Пароль (введите дважды)

**Пример:**
```
Username: admin
Email address: admin@example.com
Password: ********
Password (again): ********
Superuser created successfully.
```

> **Важно**: Запомните логин и пароль, они понадобятся для входа в административную панель.
            !!!!!!!В базе данных уже имеются некоторые учебные записи

### Шаг 6: Запуск сервера разработки

```bash
python manage.py runserver
```

После успешного запуска вы увидите сообщение:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

### Шаг 7: Открытие в браузере

Откройте браузер и перейдите по адресу:
- **Главная страница**: http://127.0.0.1:8000/
- **Административная панель**: http://127.0.0.1:8000/admin/login/

## 📖 Использование

### Публичная часть

1. **Главная страница** (`/`)
   - Просмотр списка предстоящих мероприятий
   - Поиск мероприятий по названию или месту проведения
   - Клик по карточке события для просмотра детальной информации

2. **Страница мероприятия** (`/event/<id>/`)
   - Полная информация о мероприятии
   - Дата, время, место проведения
   - Подробное описание
   - Изображение (если указано)

### Административная панель

1. **Вход в систему** (`/admin/login/`)
   - Используйте логин и пароль суперпользователя, созданного на шаге 5

2. **Управление мероприятиями** (`/admin/events/`)
   - Просмотр всех мероприятий (включая прошедшие)
   - Поиск по названию или месту
   - Добавление нового мероприятия
   - Редактирование мероприятия
   - Деактивация/активация мероприятия
   - Удаление мероприятия

3. **Добавление мероприятия**
   - Название* (обязательно)
   - Дата проведения* (обязательно, не может быть в прошлом)
   - Время начала (необязательно)
   - Место проведения* (обязательно)
   - Полное описание* (обязательно)
   - URL изображения (необязательно)
   - Статус активности (включено/выключено)

## 🔒 Безопасность

Система включает следующие меры безопасности:

- ✅ Валидация данных на клиенте и сервере
- ✅ Защита от XSS-атак (экранирование HTML)
- ✅ Проверка формата изображений (по URL)
- ✅ Аутентификация через Django (криптографический хеш паролей)
- ✅ Защита CSRF-токенами
- ✅ Доступ к админке только для staff-пользователей

## 📁 Структура проекта

```
курсач/
├── city_events/          # Основные настройки проекта Django
│   ├── settings.py      # Конфигурация приложения
│   ├── urls.py          # Главный файл маршрутизации
│   └── wsgi.py          # WSGI конфигурация
├── events/              # Основное приложение
│   ├── models.py        # Модели данных (Event)
│   ├── views.py         # Представления (views)
│   ├── forms.py         # Формы с валидацией
│   ├── admin.py         # Настройки Django admin
│   └── templates/       # HTML шаблоны
│       └── events/
│           ├── base.html
│           ├── event_list.html
│           ├── event_detail.html
│           ├── admin_login.html
│           ├── admin_events.html
│           ├── admin_event_form.html
│           └── admin_event_delete.html
├── media/               # Загружаемые файлы (если используется)
├── manage.py            # Скрипт управления Django
├── requirements.txt     # Зависимости проекта
├── .gitignore          # Игнорируемые файлы для Git
└── README.md           # Этот файл
```

## 🎨 Особенности дизайна

- Минималистичный и современный интерфейс
- Цветовая схема: голубой, зеленый, белый
- Полностью адаптивный дизайн для мобильных устройств
- Удобные размеры кнопок и элементов для касания на сенсорных экранах

## 🔧 Дополнительные команды

### Создание миграций (при изменении моделей)

```bash
python manage.py makemigrations
python manage.py migrate
```

### Запуск с другим портом

```bash
python manage.py runserver 8080
```

### Запуск с доступом из сети

```bash
python manage.py runserver 0.0.0.0:8000
```

## 📝 Примечания

- База данных SQLite создается автоматически при первом запуске миграций
- В режиме разработки (DEBUG=True) Django показывает подробные ошибки
- Для production необходимо:
  - Установить DEBUG=False
  - Настроить ALLOWED_HOSTS
  - Использовать PostgreSQL вместо SQLite
  - Настроить статические файлы (collectstatic)
  - Использовать WSGI сервер (gunicorn, uWSGI)

## 🐛 Решение проблем

### Ошибка "No module named 'django'"
Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```bash
pip install -r requirements.txt
```

### Ошибка порта уже занят
Используйте другой порт:
```bash
python manage.py runserver 8080
```

### Ошибка миграций
Попробуйте удалить файл базы данных и создать миграции заново:
```bash
del db.sqlite3  # Windows
rm db.sqlite3   # Linux/macOS
python manage.py migrate
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Версию Python (должна быть 3.10+)
2. Активировано ли виртуальное окружение
3. Установлены ли все зависимости
4. Применены ли миграции

## 📄 Лицензия

Этот проект создан в учебных целях.

---

**Успешной работы! 🚀**
